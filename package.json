{
  "name": "@spothero/arg",
  "version": "1.5.0",
  "description": "Lightweight URL argument and parameter parser.",
  "main": "lib/arg.js",
  "repository": "https://github.com/stretchr/arg.js",
  "author": "stretchr",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf lib || true",
    "build": "npm run clean && mkdir lib && babel arg.js --out-file lib/arg.js",
    "release": "npm run build && npm publish"
  },
  "devDependencies": {
    "babel-cli": "6.26.0"
  },
  "dependencies": {
    "ssr-window": "1.0.1"
  },
  "engines": {
    "node": ">=10.3.0",
    "npm": ">=6.1.0"
  }
}
